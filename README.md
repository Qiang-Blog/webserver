# webserver
## Intorduction
本项目主要通过socket网络编程实现客户端和服务器端之间的通信。客户端通过浏览器链接发起请求，服务器端对客户端发来的HTTP请求报文进行解析之后给浏览器客户端返回一个响应报文。服务器端的处理方式是使用IO多路复用中的epoll，可以同时处理多个请求，请求的解析使用预先创建好的线程池，使用模拟的Proactor模式，主线程负责监听，监听到有事件之后，从socket中循环读取数据，然后将读取到的数据封装成一个请求对象插入线程池中的请求队列中。睡眠在请求队列上的工作线程被唤醒进行处理。

## Build
make
./out